# ── 依赖 ──
node_modules/

# ── 构建产物 ──
dist/
build/
.next/

# ── 环境变量 / 密钥 ──
.env
.env.local
.env.*.local

# ── 上传 / 运行时数据 ──
uploads/
*.log
.pm2/

# ── 系统文件 ──
.DS_Store
Thumbs.db

# ── IDE & 编辑器 ──
.idea/
.vscode/
*.swp
*.swo

# ── 数据库 / 缓存 ──
*.db
*.db-journal
__pycache__/
*.pyc

# ── 临时文件 ──
*.tmp
*.temp

# ── Vite 缓存 ──
.vite/

# ── WorkBuddy 内部 ──
.workbuddy/artifacts/
.workbuddy/blobs/
.claude/
