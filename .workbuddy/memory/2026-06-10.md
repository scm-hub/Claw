# 2026-06-10 工作记录

## HRMS - 部门排序权限控制
- 需求：仅超级管理员(SUPER_ADMIN)与HR管理员(HR_ADMIN)可调整部门显示顺序
- 前端修改 `client/src/pages/departments/DepartmentTree.jsx`：
  - 引入 `useAuthStore` 获取当前用户
  - 新增 `canReorder()` 工具函数判断角色
  - `DepartmentRow` 组件增加 `showReorder` prop，非管理员隐藏上移/下移按钮
- 后端修改 `server/src/routes/departments.js`：
  - `PUT /reorder` 和 `POST /init-sort` 的 `authorize('HR_ADMIN')` 改为 `authorize('SUPER_ADMIN', 'HR_ADMIN')` 使语义更明确

## HRMS - 仪表盘员工总数修复
- 问题：仪表盘"员工总数"取 `activeEmployees`，部门管理取全部，口径不一致
- 修复：Dashboard.jsx 中 `stats?.activeEmployees` → `stats?.totalEmployees`

## HRMS - 普通员工仅查看权限
- 新建 `client/src/hooks/useCanEdit.js`：EMPLOYEE 角色返回 false，其他角色返回 true
- 前端各页面引入 useCanEdit，EMPLOYEE 隐藏：新增/编辑/删除/导入/批量导出按钮
  - EmployeeList：隐藏新增/导入/批量导出，取消行选择
  - EmployeeDetail：隐藏编辑按钮
  - EmployeeForm：非编辑角色自动跳转回详情或列表
  - DepartmentTree：隐藏新建/导入/编辑/删除（保留导出）
  - PositionList：隐藏新建/同步/编辑/删除
  - ContractList：隐藏新建/编辑/删除
  - TrainingList：隐藏新建/编辑/删除（保留报名和查看）
  - PerformanceReview：隐藏新建/编辑/删除
- 后端：leaves.js 审批接口加 authorize('HR_ADMIN', 'MANAGER')

## HRMS - 职位管理新建职位 + 停用/启用功能
- 需求：职位管理添加新建职位、停用/启用功能
- 后端新增 `PATCH /positions/:id/toggle-status` 接口：
  - 切换职位 isActive 状态
  - 停用时检查是否有在职员工，有则拒绝（返回 400）
  - 权限：HR_ADMIN 及以上
- 前端 PositionList.jsx 改动：
  - 引入 ToggleOn/ToggleOff 图标
  - 操作列新增停用/启用快捷按钮（带颜色区分）
  - 弹出确认对话框确认操作
  - 仅管理员可见（canEdit 控制）
- 修复 ConfirmDialog 组件：
  - 原组件只支持 open/title/message/onConfirm/onCancel 五个属性
  - 升级支持 content/confirmText/confirmColor/loading/onClose 扩展属性
  - 兼容所有现有调用（backward compatible）
- 修复 PageHeader 组件：
  - 原组件只支持 title/breadcrumbs，不支持 subtitle/action
  - 升级支持 subtitle（副标题）和 action（右侧操作区）
  - 所有传入 action 的按钮（新建职位、从员工同步等）现在能正确渲染
- 修复 api.del → api.delete（axios 实例没有 del 方法）
  - PositionList.jsx、RoleManagement.jsx
