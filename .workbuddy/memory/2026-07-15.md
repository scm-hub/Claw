## 2026-07-15 凌晨修复：吴月新（SALES_MANAGER）看不到采购计划

### 三个bug叠加
1. **JWT payload 没带 employeeId** → `auth.routes.js` 两个 jwt.sign 都加 `employeeId: user.employeeId || user.employee?.id || null`
2. **`hasPlanViewAccess` L130 仍传 `user.userId`** → `isPurchaserAssignee` 缓存的是 employeeId，userId 永远不匹配，导致 hasPlanViewAccess 返 false → 接口直接返空
3. **列表过滤 `where.items = { some: { allocated: false } }` 把子计划也过滤掉** → 子计划的 items 在分配时被标 allocated=true，永远不显示。改为 NOT 条件：不是(父计划且所有items都allocated) → 是子计划 OR 父计划有未分配明细

### 涉及文件
- Claw-test/xdj-scm/server/src/modules/auth/auth.routes.js（JWT payload）
- Claw-test/xdj-scm/server/src/modules/purchase/purchase.routes.js（hasPlanViewAccess + 列表过滤）
- 已重启 scm-server-test

### 教训
- hasPlanViewAccess 这种「能否访问」的检查函数，参数也必须跟 filter 函数保持一致
- where.NOT 嵌套写法：`{ NOT: { AND: [cond1, cond2] } }` = 同时满足 cond1 AND cond2 的不要

### 部门负责人数据同步
- 6个部门 managerId 全部从 HRMS 同步到 SCM 测试库
- 对接关系：王振东(人力)/孟祥营(供应链)/吕永权(商超)/苏建昌(鲜当家)/吕永宝(财务)/吴月新(餐饮)
- 效果：吴月新从1条→4条（同部门全部），数据隔离生效
- 未改代码，skill 内 isDepartmentManager + getPlanDataFilter L113 已覆盖

## 采购入库添加收发类别字段
- Schema: PurchaseReceipt 加 receiveSendTypeId 字段（存金蝶FNumber如"001"）
- 后端: /master/kingdee-receive-send-types 代理MDM（68条收发类别）
- 后端: PUT /receipts/:id/receive-send-type（实时保存）
- 后端: POST /receipts/:id/confirm 加 receiveSendTypeId 必填验证+保存
- 前端: ReceiptList.jsx 主行加收发类别下拉（Select），已确认显示文本
- 验证: 收发类别接口返回68条、确认接口有必填校验
- 涉及文件: schema.prisma、purchase.routes.js、master.routes.js、ReceiptList.jsx

### 修复 09:18 采购入库展开明细未填满
- 原因：MAIN_COL_COUNT=9 旧值，但表头现11列（加收发类别+明细数后未更新）
- 修复：MAIN_COL_COUNT=11
- 涉及文件：ReceiptList.jsx

## 正式环境发布（09:25）
- 口令 pK!(*&1987) 确认
- 5项功能发布：收发类别字段+下拉、展开列宽对齐、JWT employeeId、到货日期禁用修复
- DB: purchase_receipts 加 receive_send_type_id 字段
- 部门 managerId 正式库已全部正确，无需同步
- 验证：68条收发类别/纯名称展示/确认接口校验 ✅
- 涉及文件：schema.prisma、auth.routes.js、purchase.routes.js、master.routes.js、ReceiptList.jsx、PurchasePlanList.jsx
- dist rebuild + scm-server restart

### 修复 09:30 正式环境新增采购订单报错
- 错误：Unknown field `name` for select statement on model `User`
- 根因：发布时漏改正式 schema.prisma 的 PurchasePlan.assignee 关联——仍是 `User?` 但代码按 Employee 查询
- 修复：
  - schema: assignee `User?` → `Employee?`；删 User.assignedPurchasePlans + 加 Employee.assignedPurchasePlans
  - 删旧 FK → db push → generate → 重启 scm-server
- 验证：available-plans 接口正常返回 success，修好后不会再报错

### 金蝶物料等级数据获取（BD_AuxPtyValue API）
- 用户提供了新的金蝶接口：FormId=BD_AuxPtyValue, Filter=FMaterialAuxPropertyId.FNumber='wldj'
- 新增 pullMaterialGradeValues() 方法，返回物料编码→等级列表 Map
- pullMaterialsWithGrades() 改为调用新 API 获取真实等级（替代之前置空的做法）
- 全量同步 231 条物料，0 失败
- 验证：香菇 13 个等级（花菇/白面菇/出口菇/一级~四级菇/大片菇/残片菇/菇蕾菇/疏蕾菇/菇根/迷你香菇）全部正确
- 平菇 2 个等级（一级菇/二级菇），其他变体编码暂无独立等级（金蝶实际数据）
- 涉及文件：Claw-test/mdm/server/src/services/kingdee-adapter.js

### 修复 10:10 MDM 物料等级列表显示"未设置"
- 错误：截图显示香菇/平菇等级列都是"未设置"
- 根因：/kingdee/data 和 /kingdee/organizations 端点硬编码 database: mdm_db，测试环境用 mdm_db_test
  查询落到正式库旧数据，没有 grades 字段
- 修复：两处都改为从 process.env.DATABASE_URL 解析库名
- 验证：香菇 13 个等级 / 平菇 2 个等级 / 变体 0（变体在金蝶中无独立等级）
- 涉及文件：Claw-test/mdm/server/src/routes/kingdee.js（2处）
- 教训：跨库直连的端点必须从环境变量读库名，不能硬编码

### 客户管理地址字段改为高德地图搜索 15:42
- 将新增客户界面的"地址"TextField改为autocomplete，对接高德 inputtips API
- 复用现有 /api/address/search 代理（无需新增后端）
- 用户输入≥2字符后防抖300ms查询高德，下拉显示地址名称+详细地址
- 选中后自动填入完整地址，也可手动输入（freeSolo）
- 涉及文件：Claw-test/xdj-scm/client/src/pages/master/CustomerList.jsx
- 构建通过，重启scm-server-test
### 仓库管理地址字段改为地图选点 16:50
- 与客户管理同样的 MapPicker 实现
- 涉及文件：Claw-test/xdj-scm/client/src/pages/master/WarehouseList.jsx
### 销售订单目的地字段改造 18:05\n- Schema: SalesOrder 加 destinationAddress 字段 + db push\n- 后端: create/update 路由接受 destinationAddress\n- 前端 SalesOrderList.jsx:\n  - 选客户自动反填客户地址到目的地\n  - 目的地从下拉改为 TextField + MapPicker 地图选点\n  - 选仓库后调用 /address/calc-distance 高德算距自动填公里数\n  - 保存校验 destinationAddress 必填\n- 涉及文件: schema.prisma, sales.routes.js, SalesOrderList.jsx\n- 构建通过，scm-server-test 已重启\n
## 正式环境发布（20:09）
- 口令 pK!(*&1987) 确认
- 6项功能发布：
  1. 销售订单目的地改造（schema + 后端 + 前端）
  2. 物料等级/产品组菜单移除
  3. 客户管理地址必填+地图选点
  4. 仓库管理地址必填+地图选点
  5. MDM物料等级数据获取（BD_AuxPtyValue）
  6. MDM数据库名硬编码修复
- DB: sales_orders 加 destination_address 字段
- 统计: 9文件 + db push + generate + dist rebuild + scm-server/mdm-server重启

### 正式环境 MDM 金蝶物料等级修复 20:13
- 问题：正式环境 MDM 金蝶物料列表中等级没有获取到
- 根因：kingdee-sync.js `pullMaterialsFromKingdee` 调用的是 `adapter.pullMaterials()`（旧方法，无等级），而不是 `adapter.pullMaterialsWithGrades()`（新方法，通过 BD_AuxPtyValue 获取等级）
- 测试环境已修复（之前漏了同步到正式环境的 kingdee-sync.js）
- 修复：
  1. kingdee-sync.js L140: `adapter.pullMaterials(lastSync)` → `adapter.pullMaterialsWithGrades(lastSync)`
  2. extra JSON 添加 `grades: record.grades || []`
  3. 重启 mdm-server
  4. 全量拉取 231 条物料
- 验证：香菇 13 个等级、平菇 2 个等级 ✅
- 涉及文件：Claw/mdm/server/src/services/kingdee-sync.js (1行变更+1字段)

### 测试环境 SCM 产品管理界面改造 20:39
- 要求：去掉分类字段，新增本地销售单位和本地销售换算系数，必填+*
- Schema: Material 模型添加 localSalesUnit(String?) + localSalesConversionFactor(Decimal, 默认1)
- 前端 MaterialList.jsx 修改：
  - 去掉 category 状态变量、loadCategories、查询筛选、表格列、弹窗字段
  - 新增 localSalesUnit（标签加* + 必填验证）和 localSalesConversionFactor（标签加* + 必填验证）
  - 表格新增"本地销售单位"和"本地换算系数"两列（colSpan 17→18）
  - 表头/行去掉分类、新增两列
  - 表单默认值、保存逻辑同步更新
- db push + prisma generate + dist build + scm-server-test 重启 ✅
- 涉及文件：schema.prisma, MaterialList.jsx

### 修复编辑产品 loadCategories 报错 20:47
- 错误：编辑产品点保存时弹窗"loadCategories is not defined"
- 根因：之前删除分类字段时，handleSave 内的 loadCategories() 调用忘了删
- 修复：删除 handleSave 中的 loadCategories() 调用
- 重新构建 dist + 重启 scm-server-test ✅
- 涉及文件：Claw-test/xdj-scm/client/src/pages/master/MaterialList.jsx L241
- 教训：删除函数时全局搜一遍调用点（grep 函数名）

### 修复本地销售单位必填提示不显示 20:51
- 问题：localSalesUnit 未填时保存无红色边框/错误提示
- 根因：TextField 缺少 `error={!!formErrors.localSalesUnit}` + `helperText={formErrors.localSalesUnit || ''}`
- 修复：对齐 localSalesConversionFactor 的写法，补上 error 和 helperText
- 涉及文件：Claw-test/xdj-scm/client/src/pages/master/MaterialList.jsx L638
- 重新构建 + 重启 ✅

### 采购订单展开明细单位改为采购单位 20:57
- 要求：采购订单展开明细中的"单位"列显示采购单位而不是基准单位
- 修改 3 处：列表展开行(L599)、编辑弹窗明细(L865)、详情弹窗明细(L1013)
- 全部改为 `material.purchaseUnit || material.unit`（优先采购单位，fallback基准单位）
- 涉及文件：Claw-test/xdj-scm/client/src/pages/purchase/PurchaseOrderList.jsx
- 重新构建 + 重启 ✅

### 采购入库确认改为先推金蝶再入库 21:25
- 根因：RC20260715P93XUG 金蝶同步失败，供应商 040122379 在采购组织 05180 下未授权
- 改造：确认入库 → 先同步推金蝶 → 成功才插入出入库记录 → 失败不插入
- 后端改动：
  - Schema: purchase_receipts 新增 kingdee_sync_message TEXT 字段
  - 新增 pushToKingdee() 纯函数，返回 {success, poNo, inboundNo, error}
  - 新增 retrySyncReceiptToKingdee() 用于已确认入库单的重试
  - handleConfirmReceipt 重构：金蝶推送提到第4步（入库之前），失败返回错误不执行入库
  - 新增 POST /receipts/:id/retry-kingdee 重试接口
  - 废弃 async syncReceiptToKingdee，移除 SYNCING 状态
- 前端改动：
  - FAILED chip tooltip 显示 kingdeeSyncMessage 具体错误
  - 新增 RefreshIcon 重试按钮（仅 FAILED 状态显示）
  - 移除 SYNCING 轮询逻辑（不再异步推送）
- 涉及文件：schema.prisma, purchase.routes.js, ReceiptList.jsx

### 多组织隔离 + 右上角组织切换下拉框 22:13
- 需求：物料按组织隔离 → 6张表加 orgCode 字段 → 基础数据按组织过滤 → 金蝶推送用 receipt.orgCode
- Schema 改动（6表加 orgCode String? + @@index）：
  - Warehouse / Material / Supplier / Customer / PurchaseOrder / PurchaseReceipt
  - 老数据从 code 前5位回填：warehouse(05180/05570/10001), supplier(04012:3条), material(05020:6/05021:2), order/receipt(05180)
- 后端新增：
  - `orgContext.js` 中间件：读 X-Org-Code 头→写入 req.orgCode
  - `getOrgFilter(req)` 工具函数：返回 Prisma where 条件
  - `GET /api/master/organizations`：从 mdm_db 查组织列表（自动区分 test/prod DB）
  - `index.js`：`app.use('/api', orgContext)` 全局挂载
- API 改造（orgCode 过滤 + 自动填充）：
  - materials/suppliers/warehouses/customers 的 GET list：加 `Object.assign(where, getOrgFilter(req))`
  - materials POST：`if (!data.orgCode && req.orgCode) data.orgCode = req.orgCode`
  - purchaseOrder/receipt POST：创建时 `orgCode: req.orgCode || null`
  - handleConfirmReceipt 确认时写入 orgCode
  - pushToKingdee：接受 `orgCodeParam` 参数，优先用显式传入的组织编码
- 前端改动：
  - api.js：全局加 `X-Org-Code` header（从 localStorage `scm_current_org` 读取）
  - 暴露 `setCurrentOrg()` / `getCurrentOrg()` 工具函数
  - MainLayout.jsx：右上角用户图标左边加 Select 下拉框
  - 数据源 `/api/master/organizations`（orgCode + orgName）
  - 单组织时隐藏下拉框；多组织时显示
  - 切换组织时写 localStorage + 刷新页面
- 验证结果：
  - 不带 X-Org-Code：SUPER_ADMIN 看全部 8 个物料
  - X-Org-Code:05020：6 个物料（05020 开头）
  - X-Org-Code:05021：2 个物料（05021 开头）
- 涉及文件：schema.prisma（6模型）, orgContext.js（新）, index.js, master.routes.js, purchase.routes.js, api.js, MainLayout.jsx
- 当前组织数据：只有 1 个组织（30001 淄川河润），前端下拉框隐藏
