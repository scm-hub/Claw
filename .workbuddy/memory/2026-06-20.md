# 2026-06-20 工作日志

## SCM 销售计划逻辑改造

### 需求
- 业务员提报销售计划改为"针对每个客户、多种产品报计划"
- 去掉金额字段（targetAmount / planAmount）
- 计划类型新增"周计划"(WEEKLY)
- "销售代表"字段改名为"提报人"
- 提报人根据当前登录用户自动反填，不可编辑

### 修改文件
1. **后端 `xdj-scm/server/src/modules/sales/sales.routes.js`**
   - POST `/plans`：salesRepId 从 `req.user.employeeId` 获取，不再从 body 接收
   - PUT `/plans/:id`：去掉 salesRepId 和 targetAmount 的更新
   - 创建/更新 items 时去掉 planAmount（schema 默认值 0 保留）

2. **PC 端 `xdj-scm/client/src/pages/sales/SalesPlanList.jsx`** — 完全重写
   - 引入 `useAuthStore` 获取当前登录用户
   - 提报人字段：只读 disabled，显示当前用户员工姓名
   - 计划类型增加 WEEKLY（周计划）
   - 去掉 targetAmount/actualAmount/completionRate
   - 表单新增明细编辑区：客户下拉 + 产品下拉 + 数量 + 备注，可增删行
   - 列表表头去掉金额列，增加"周期"和"明细数"列
   - 详情弹窗展示客户+产品+数量+备注

3. **移动端 `xdj-scm/mobile/src/pages/SalesPlans.jsx`** — 同步改造
   - 去掉金额相关（汇总卡片改为明细总数、详情去掉金额列）
   - 创建表单增加客户选择和计划类型选择
   - 提报人自动反填只读
   - "销售员"→"提报人"

### 验证结果
- API 测试通过：创建计划时 salesRepId 自动填充为当前用户 employeeId ✅
- targetAmount 默认为 0 不再使用 ✅
- 明细正确保存客户+产品+数量 ✅
- PC 端和移动端构建均成功 ✅

## 销售计划第三轮调整：去掉库存显示 + 明细加单位

### 改动
- PC 端 `SalesPlanList.jsx`：移除 stockMap state / loadStockMap / 库存 helperText / MenuItem 中的 [库存xxx] 后缀；明细表头加"单位"列，选中产品后自动显示 unit
- 移动端 `SalesPlans.jsx`：移除 stockMap / loadOptions 中的库存加载 / Alert 文案去掉"查看实时库存"；产品下拉去掉库存信息；计划数量 label 带上单位 `(袋)` 等
- 库存显示功能保留给后续销售订单场景使用
- 两端构建均成功 ✅

## SCM 销售计划二次改造（用户追加需求）

### 需求
- 标题默认自动生成：`{当前用户}-{年}年{月}月第{周}周-{计划类型}`（切换类型自动重算）
- 部门字段不可编辑（自动填充当前用户所属部门）
- 去掉开始日期和结束日期（后端根据 planType 自动计算周期）
- 明细每行加上"发货时间"（deliveryDate）
- 选择产品时显示实时库存（可用量/总量）

### 修改文件
1. **`xdj-scm/server/prisma/schema.prisma`** — SalesPlanItem 加 `deliveryDate DateTime?`
2. **`xdj-scm/server/src/modules/sales/sales.routes.js`**
   - POST/PUT：不传 periodStart/periodEnd，后端按 planType 自动算周期
   - items create 加 `deliveryDate`
3. **`xdj-scm/client/src/pages/sales/SalesPlanList.jsx`** — 重写
   - `genTitle()` 根据用户名+计划类型自动生成标题
   - 部门 disabled，计划类型变化时自动重算标题
   - 去掉日期选择器
   - 明细加发货时间列 + 产品下拉显示库存（`[库存XXXkg]`）
   - 打开弹窗时 loadStockMap() 聚合库存
4. **`xdj-scm/mobile/src/pages/SalesPlans.jsx`** — 同步改造

### 验证结果
- API 测试：创建不传日期，后端自动算 WEEKLY 周期(周一~周日) ✅
- deliveryDate 正确保存和返回 ✅
- 库存显示：木耳干品可用120袋 ✅
- 两个前端构建成功 ✅
