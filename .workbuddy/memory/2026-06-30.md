# 2026-06-30 工作日志

## 成本价计算公式修改
- CostPriceRecord 新增 `lossQty` 字段（Decimal(12,2)，最新盘点损耗数量）
- 结存数量公式从 `期初+入库-出库` 改为 `期初+入库-出库-损耗`
- 损耗数量来源：取本周期内（periodStart~now）已完成盘点的损耗合计（修复了重复扣减 bug）
- Bug 修复：原来取最新一条盘点损耗，每次手动计算都重复扣同一条损耗 → 改为按盘点completedDate在periodStart~now区间过滤
- cost-price.scheduler.js 算法步骤从6步改为7步（新增第5步损耗）
- 前端成本价历史表格新增"损耗数量"列，详情摘要也添加损耗数量
- 后端 /cost-price/:id/movements summary 和 getLatestCostPrice 都返回 lossQty

## 成本价计算逻辑说明（重要）
- 定时任务每天0点/12点全量增量计算（`onlyNew: false`），每个物料新增一条记录
- `periodStart = lastRecord.calculatedAt`，取上次到本次的增量出入库数据
- 无历史记录时 `periodStart` 取最近一个定时任务时间（00:00 或 12:00），不再取全量历史
- 出库数量排除 `STOCK_TAKE_ADJUST` 盘点调整，损耗单独从 StockTakeItem 取
- 手动"立即计算"按钮已改为全量计算（`onlyNew: false`），不再仅算新物料

## 费用登记改进
- 费用登记卡片页新增"规格"和"单位"列（后端 select 加 spec，返回数据加 spec）
- 删除按钮改为所有行可见（不再仅 hasAnyFee 时显示）
- 新增费用对话框改为同时录入三类费用金额（去掉费用类型下拉和费用名称字段）

## 采购计划智能建议添加编号列
- 后端 `/plans/suggest` 返回每条建议新增 `suggestionNo` 字段
- 编号规则改为类似计划编号：PS+YYYYMMDD+随机6位字母+3位序号，如 `PS20260630XK2M3Q001`
- 每次生成建议时先生成一个批次号(genNo('PS'))，排序后追加3位序号
- 前端智能建议弹窗表格新增"编号"列，monospace字体显示

## SCM业务数据全量清空
- 清空28个业务数据表（采购/销售/仓储/物流/财务/成本价等）
- 保留基础数据（物料、供应商、客户、仓库、员工、用户等）
- 库存表保留记录但重置qty和lockedQty为0
- 关闭外键检查后按子表→主表顺序删除，避免约束冲突
- PurchasePlanItem schema 新增 `suggestionNo` 字段（String?，映射 suggestion_no 列）
- `/plans/suggest` 查询已有 suggestionNo 且采购计划状态未完成（DRAFT/PENDING/APPROVED/IN_PROGRESS）的明细，排除这些物料不再显示
- `/plans/from-suggestion` 创建明细时传入 suggestionNo
- 前端生成采购计划时传递 suggestionNo 到后端
- 采购计划展开明细表新增"建议编号"列

## 产品组（MaterialGroup）功能实现
- Schema 新增 `MaterialGroup` 模型（code/name/category/description/status，映射 material_groups 表）
- Material 新增 `groupId` 外键关联 MaterialGroup（可选，ON DELETE SET NULL）
- 后端新增 5 个 MaterialGroup CRUD API（列表含物料数/详情含物料列表/创建/更新/删除）
- 物料列表 API 新增 groupId 筛选参数，返回 group 关联信息
- 物料创建/更新 API 支持 groupId 字段（空字符串转 null）
- 前端新增 MaterialGroupList.jsx 页面（列表+展开关联物料+CRUD弹窗）
- MaterialList.jsx 适配：新增产品组筛选下拉、表格"产品组"列（Chip显示）、编辑弹窗可选产品组
- 打印模块列表新增"产品组管理"
- 菜单新增"产品组管理"（在"产品管理"之前）

## 采购计划数据隔离（方案三：混合隔离）实现完成
- `purchase.routes.js` 新增 `getPlanDataFilter(user)` 和 `hasPlanViewAccess(user)` 两个工具函数
- 12个 API 路由改造完成（plans/children/suggest详情/创建/更新/审批/驳回/提交/删除/分配/转发/available-plans）
- SUPER_ADMIN看全部、PURCHASE_MANAGER看本部门、PURCHASE_STAFF看自己分配+草稿、其他角色无数据
- 端到端测试四种角色数据隔离全部通过
- available-plans 路由因编辑导致代码结构损坏，完全重写修复

## 定时任务 departmentId 修复
- 旧定时任务创建计划时 departmentId 取了 SUPER_ADMIN 的部门（总经办），导致 PURCHASE_MANAGER 看不到
- 修复：优先找 PURCHASE_MANAGER 作为创建者（取其 departmentId），SUPER_ADMIN 作为降级
- purchase-plan.scheduler.js 中 adminUser 查询改为分两步（先 PURCHASE_MANAGER 后 SUPER_ADMIN）
- 创建父计划时显式设置 `departmentId` 字段
- 已修复历史遗留数据4条（1个父计划+3个子计划的 departmentId 从总经办改为餐饮事业部）

## 子计划 departmentId 取采购员部门（不再继承父计划部门）
- 修改分配路由 `/plans/:id/allocate`：assignment.user.employee include 加 departmentId，子计划 departmentId 取 `assignment.user.employee?.departmentId || parentPlan.departmentId`
- 修改转发路由 `/plans/:id/forward`：targetUser.employee include 加 departmentId，转发计划 departmentId 取 `targetUser.employee?.departmentId || sourcePlan.departmentId`
- 修改定时任务 `purchase-plan.scheduler.js`：assignment.user.employee include 加 departmentId，子计划 departmentId 取 `assignment.user.employee?.departmentId || parentPlan.departmentId`
- 修复现有数据3条：吕永权(2条)和王小明(1条)的子计划 departmentId 从餐饮事业部改为商超事业部
- 设计逻辑：fallback 到父/源计划部门，防止采购员无部门信息时 departmentId 为空

## 采购计划三字段 + 确认流程 + 采购订单过滤
- PurchasePlanItem 新增 `unitPrice`(Decimal(12,2)) 和 `actualQty`(Int) 字段，supplierId 已存在
- PurchasePlan 状态新增 CONFIRMED（String 类型，不需要改 schema 枚举）
- 后端新增两个路由：
  - PUT `/plans/:id/confirm` — 确认采购计划（校验每条明细 supplierId/unitPrice/actualQty 填写完整，仅 APPROVED+assigneeId=当前用户可确认）
  - PUT `/plans/:id/items` — 保存采购计划明细字段（供应商/单价/实际采购数量，仅 APPROVED+assigneeId=当前用户可修改）
- available-plans 查询改为只查 CONFIRMED 状态（原来查 APPROVED）
- remainingQty 计算改为 actualQty - orderedQty（原来是 planQty - orderedQty）
- 前端 PurchasePlanList.jsx：
  - STATUS_MAP 加 CONFIRMED（蓝色 info）
  - 行操作按钮：APPROVED 子计划显示"填写采购详情"和"确认"按钮（仅 assigneeId=当前用户可见）
  - 弹窗改造：APPROVED 子计划打开时标题为"填写采购详情"，明细表加供应商/单价/实际采购数量三列，物料和计划数量只读，保存按钮改为"保存采购详情"
  - 确认弹窗：二次确认提示
- 前端 PurchaseOrderList.jsx：
  - 新增采购订单提示改为"需要已确认且未完全下单的采购计划"
  - 明细表加"供应商"列，"计划量"改为"实际采购量"，"预算单价"改为"确认单价"
  - unitPrice 取值优先用 planItem.unitPrice（实际单价），fallback 到 budgetUnitPrice
  - 选中明细后自动填充供应商（如果订单供应商未选择）
