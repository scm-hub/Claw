# 2026-07-14 工作日志

## 正式+测试环境双启动
- 同时启动了正式环境(Claw/)和测试环境(Claw-test/)的全部 PM2 服务
- 正式环境 7 个服务：portal-server(4001), hrms-server(4002), scm-server(4003), ai-server(4004), mdm-server(4005), workflow-engine(4011), gateway(5174)
- 测试环境 7 个服务：portal-server-test(14001), hrms-server-test(14002), scm-server-test(14003), ai-server-test(14004), mdm-server-test(14005), workflow-engine-test(14011), gateway-test(15174)
- 发现 ecosystem.config.js 因 package.json 的 "type":"module" 导致 PM2 无法加载（CommonJS 语法冲突），改用 `pm2 start src/index.js --name X --cwd Y --interpreter /path/to/node` 逐个启动
- 初次启动出现重复进程（每个服务两个），原因是 `pm2 start $NODE --name X` 语法不正确导致 PM2 创建了两个实例；修正为 `--interpreter` 参数后解决
- 测试环境 start-all-pm2.sh 的 BASE 路径仍指向 Claw（未改为 Claw-test），端口也是正式端口，需后续修复
- PM2 配置已 save 持久化，共 14 个进程全部 online
