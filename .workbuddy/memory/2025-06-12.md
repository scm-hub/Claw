# 2025-06-12 HRMS 工作记录

## 请假审批流程改造 - 部门经理审批

### 需求
将请假审批从"任意 HR_ADMIN/MANAGER 审批"改为"申请人所在部门的经理审批"。

### 关键问题：部门经理自己请假
王振东（EMP010）是"经理室"的部门经理，他自己提交的请假申请无法由自己审批。
- 后端 `getDepartmentManager` 增加 `excludeEmployeeId` 参数，排除申请人自己
- 审批列表过滤掉自己提交的请假（`NOT: { employeeId: managerEmployeeId }`）
- 前端显示"您是本部门的经理，您的请假将由管理员审批"
- `needsAdminApproval` 标识返回给前端

### 修改文件
- `server/src/services/leave.service.js` — getDepartmentManager 增加 excludeEmployeeId；applyLeave 传 excludeEmployeeId；getPendingLeaves 过滤自己请假；approveLeave 校验不能审批自己
- `server/src/routes/leaves.js` — /approver 接口返回 needsAdminApproval；审批错误处理增加新错误类型
- `client/src/pages/leaves/LeaveApply.jsx` — 审批流程卡片增加 needsAdminApproval 提示
- `client/src/pages/leaves/LeaveBalance.jsx` — 审批人列增加"管理员审批"标识
