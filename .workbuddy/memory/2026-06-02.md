# 2026-06-02 工作记录

## 人力资源管理系统（HRMS）开发

### 项目概况
- 按标准 SOP 工作流完成：PRD → 架构设计 → 代码实现
- 技术栈：React 18 + Vite + MUI + Tailwind CSS（前端），Express.js + Prisma + MySQL（后端）
- 项目路径：`/Users/pukun/WorkBuddy/Claw/hrms/`

### 已完成
1. **PRD 文档**：10个P0用户故事 + 14个功能点 + UI设计要点
2. **架构设计**：前后端分离架构、数据模型、API设计、任务分解
3. **后端代码**：10个路由模块 + 8个服务模块 + 3个中间件 + 薪资/个税计算工具
4. **前端代码**：15个页面组件 + 5个通用组件 + 状态管理 + 路由守卫
5. **数据库**：MySQL 9.6.0，10 张业务表
6. **Bug 修复**：Login.jsx 导入路径、useAuth 错误处理、Prisma Client 缓存问题
7. **注册/初始化功能**：支持空数据库时注册首个管理员，系统自动检测初始化状态
8. **部门管理增删改查**：后端增加 GET/flat、GET/:id、DELETE 接口；前端重写为表格+树形展开+弹窗表单+删除确认
9. **部门人数点击跳转**：部门管理点击人数 → 跳转员工列表并按部门筛选；员工列表新增部门筛选下拉框
10. **员工编辑保存修复**：前端不再传递关联对象(department/user/contracts)；后端增加字段白名单过滤，只允许合法标量字段传入 Prisma
11. **员工新增保存修复**：departmentId 空字符串导致外键约束失败；前端增加必填校验+只提交合法字段+类型转换；后端增加校验+AppError 友好报错
12. **P1/P2 功能扩展**：合同管理（CRUD+到期提醒+类型筛选）、绩效评估（完整CRUD+评分可视化+状态流转+统计卡片）、培训管理（课程CRUD+员工报名/取消+名额管理+报名详情）
13. **数据库新增模型**：Training（培训课程）、TrainingEnrollment（培训报名），Contract/Performance 增加 status 等字段
14. **员工导入导出功能**：后端 GET /export（按筛选条件导出Excel）、GET /import/template（下载导入模板）、POST /import（Excel批量导入）；前端添加导出按钮、导入弹窗（含上传、结果展示、错误行号提示）

### 数据库配置
- **数据库**：MySQL 9.6.0（Homebrew），数据库名 `hrms`，字符集 utf8mb4
- **连接**：root / Scm@2025! @ localhost:3306
- **12 张表**：User, Department, Employee, Attendance, Leave, SalaryRecord, Job, Candidate, Performance, Contract, Training, TrainingEnrollment

### 当前运行服务
- **最新服务**：http://localhost:4001 — 后端+前端一体（包含多选+批量导出功能）
- **旧服务**：http://localhost:4000（残留进程，需用户手动 kill）
- **旧后端**：3003~3010 端口均有残留进程
- **旧前端**：5173, 5174 端口有残留 Vite 进程
- **MySQL 连接数问题**：旧 node 进程占用大量 MySQL 连接（150+），需用户手动清理

### 注意事项
- MySQL 中 Leave 是保留字，查询需用反引号 \`Leave\`
- npm 安装需要使用 `--registry=https://registry.npmmirror.com --cache=/tmp/npm-cache`
- 沙箱限制无法 kill 旧进程，需用户手动执行 `kill -9 <PID>`
- 当前管理员账号：admin@hrms.com / admin123（通过注册功能创建）
- **重要**：生产模式已部署，前端通过 `server/src/app.js` 中 `express.static` 提供 `client/dist` 目录的静态文件，无需 Vite 代理
- 修复了 reports 路由未在 app.js 中注册的问题
- 修复了 SalarySlip.jsx 中缺少 Grid 导入的问题
- 添加了 ErrorBoundary 到 main.jsx 捕获 React 渲染错误
- **重大修复（第二轮）**：
  - `useFetch.js`：修复 blob 响应处理，导出文件时 interceptor 现在返回完整 response 对象；blob 请求的错误会正确解析 JSON 错误信息
  - `Dashboard.jsx`：移除 `echarts-for-react` 依赖（可能是渲染崩溃的根源），改为纯 CSS 横向柱状图
  - `Reports.jsx`：修复为真正下载文件（员工花名册→Excel下载，考勤报表→CSV下载）
  - `EmployeeList.jsx`：修复导出 URL（去掉多余的 `/api` 前缀）、修复 blob 下载逻辑（适配新 interceptor）
  - 构建体积从 1.7MB 降至 640KB（去掉 echarts）
- **功能增强（第三轮）**：
  - `DataGrid.jsx`：添加多选功能（selectable/selectedIds/onSelectionChange/selectionToolbar），支持全选/取消全选/单行选择
  - `EmployeeList.jsx`：添加员工多选 + 批量导出按钮（选中员工后出现"批量导出(N)"按钮）
  - `employee.service.js`：exportEmployees 支持 `ids` 参数，按指定员工 ID 列表导出
  - `employees.js`路由：export 接口支持 `ids` 查询参数
  - `.env`：DATABASE_URL 增加 `connection_limit=5&pool_timeout=10` 限制 Prisma 连接池
  - 创建了 `/opt/homebrew/etc/my.cnf` 设置 MySQL max_connections=500
