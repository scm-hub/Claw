# 2026-06-04 工作日志

1. **考勤打卡规则重构** - 从"上班打卡+下班打卡"两个按钮改为单按钮多次打卡模式
   - Prisma新增ClockRecord模型，记录每次打卡明细
   - Attendance模型新增punchCount字段和clockRecords关联
   - 后端合并clock-in/clock-out为单一punch接口，每次打卡自动更新：最早时间=上班卡clockIn，最晚时间=下班卡clockOut
   - 旧接口clock-in/clock-out内部重定向到punch保持兼容
   - 新增LATE_EARLY状态（既迟到又早退）
2. **考勤打卡页面UI重构** - AttendanceClock.jsx全面重写
   - 单一"打卡"大按钮替代上下班两个按钮
   - 上班卡/下班卡信息展示区（左侧绿色上班图标，右侧蓝色下班图标）
   - 今日打卡记录列表（显示每次打卡时间，标记上班卡/下班卡）
   - 底部提示"上班卡=当日最早打卡时间，下班卡=当日最晚打卡时间"
3. **侧边栏导航一致性修复** - MainLayout.jsx重构支持子菜单（Collapse）
   - "考勤管理"作为父菜单，包含子项"考勤打卡"和"考勤记录"
   - 子菜单自动展开/折叠，当前路径匹配时自动展开
   - 父菜单选中用浅蓝色，子菜单选中用深蓝色
4. **考勤记录页更新** - AttendanceRecords.jsx新增"打卡次数"列和LATE_EARLY状态
5. **考勤记录导出功能** - 后端新增`GET /api/attendance/export?month=YYYY-MM`，导出两个Sheet的Excel：①考勤汇总（日期/工号/姓名/部门/上班时间/下班时间/打卡次数/状态）②打卡明细（每条打卡记录，标记上班卡/下班卡/中间打卡）。前端月份选择器旁新增「导出Excel」按钮
6. **员工离职工作交接流程** - 完整实现离职交接机制
   - Prisma Employee模型新增handoverNote字段（交接说明）
   - 后端：状态改RESIGNED时自动检查是否部门负责人，是则必须传newManagerId，否则返回400错误；自动设置leaveDate为当天；新负责人必须为在职员工；交接后自动更新所有管理部门的managerId
   - 前端EmployeeForm：选择"离职"后展开红色离职交接区域（负责人时弹出对话框必须选新负责人+Autocomplete模糊搜索在职员工+离职日期+交接说明）
   - 前端EmployeeDetail：离职员工显示离职日期和交接说明
   - 部门负责人选择：只显示在职员工(status=ACTIVE)
7. **部门负责人选择限制** - DepartmentTree.jsx获取员工列表时加status=ACTIVE筛选，只有在职员工可选为部门负责人
8. **部门表单必填字段** - 新建/编辑部门时，部门名称、上级部门、部门负责人均设为必填
   - 前端：三个字段加required标识，handleSubmit增加parentId和managerId的空值校验，上级部门下拉默认项改为disabled"请选择上级部门"
   - 后端：POST和PUT路由增加parentId和managerId的必填校验
