# 2025-06-24 工作日志

## 入库管理 — 5条PENDING入库单明细补全
- **问题**：5条 PENDING 状态入库单（RC20260623XT211Y/D3CL40/09XZIP/HBF33M/QYPXA7）没有明细数据，是旧代码遗留问题
- **原因**：旧版创建入库单的逻辑是每个物料创建一条 receipt（单头+无明细），新版改为一条 receipt 含 N 条 items
- **修复**：根据每条 receipt 的 materialId 匹配对应 PO item，创建 PurchaseReceiptItem 记录
  - RC20260623XT211Y → 猴头菇 (PO1, qty:1000)
  - RC20260623D3CL40 → 香菇鲜品 (PO2, qty:400)
  - RC2026062309XZIP → 精品澳洲牛排 (PO2, qty:1520)
  - RC20260623HBF33M → 金针菇鲜品 (PO2, qty:300)
  - RC20260623QYPXA7 → 鹿茸菇 (PO2, qty:700)
- 所有 receipt item 的 receivedQty=0, status=PENDING, 可在前端正常入库确认

## 采购订单 — 单价为0校验
- **需求**：新增采购订单时，如果所选物料中有单价为0的，点击创建时提示
- **改动文件**：client/src/pages/purchase/PurchaseOrderList.jsx
- **改动点**：
  1. `handleCreateOrder` 函数：在数量校验之后、API调用之前，过滤 unitPrice 为0的明细，用 setCreateError 提示物料名称
  2. `handleSaveEdit` 函数：同步加上相同校验，用 alert 提示
- 已重新构建前端（VITE_MODE=gw vite build），preview 模式刷新即生效

## 出入库记录 — 新增 movementDate 字段
- **需求**：出入库记录列表的"日期"应显示业务日期（入库日期），而非系统创建时间
- **改动**：
  1. schema：StockMovement 新增 `movementDate DateTime @default(now())`
  2. 后端5处 stockMovement.create 全部加上 `movementDate`：
     - 采购入库（2处）：用 `receipt.receiptDate`
     - 销售出库、手动出入库、盘点调整：用 `new Date()`
  3. 查询列表 orderBy 改为 `movementDate: 'desc'`
  4. 前端列表改为显示 `item.movementDate`（原 `item.createdAt` 不再显示）
  5. 6条历史数据已回填：PURCHASE_RECEIPT 类型的 movementDate 从对应 receipt.receiptDate 取值
- 前端已重建，后端已重启

## 成本价计算历史 — 默认显示各物料最新结果
- **需求**：
  1. 默认显示各物料最后一次计算结果（每个物料只显示最新一条）
  2. 通过查询条件（物料/日期范围）切换到完整历史模式
  3. 标题"成本价计算历史"右侧加红色小字"每天0点和12点自动计算"
- **后端改动**（wms.routes.js）：
  - history API 新增 `latestOnly` 参数：用 raw SQL JOIN 子查询取每个物料 MAX(calculatedAt) 的记录
  - 新增 `startDate`/`endDate` 日期范围查询
- **前端改动**（PriceListPage.jsx）：
  - history state 增加 `startDate`/`endDate`
  - `isQueryMode` = 有任意查询条件时为 true，此时不加 `latestOnly` 参数，显示全部历史
  - 默认（无查询条件）传 `latestOnly=true`，每物料只显示最新一条
  - 标题旁加红色 Typography span
  - 查询区增加开始日期、结束日期两个 date picker
  - 默认模式下显示提示文字"当前显示各物料最新计算结果，设置查询条件可查看历史明细"

## [14:59] 销售订单列表加业务员列
- 表头在"客户"和"订单日期"之间加"业务员"列
- 数据取 r.salesRep?.name，无则为"-"
- 后端 GET /orders 已 include salesRep，无需改后端
- 前端已重建

## [15:22] 销售订单审核流程改造
- **Schema**: SalesOrder 加 `approvedById` + `approvedAt` + `approvedBy` 关联 Employee
- **状态流转**: DRAFT→PENDING_APPROVAL, CONFIRMED→APPROVED
  - 新建订单状态从 DRAFT 改为 PENDING_APPROVAL（待审核）
  - 旧 CONFIRMED 改为 APPROVED（已审核）
  - 已有数据迁移：1条 DRAFT→PENDING_APPROVAL，1条 CONFIRMED→APPROVED
- **后端新增**: PUT /orders/:id/approve 审核接口，记录审核人+审核时间
- **后端修改**: 编辑/删除权限从 DRAFT 改为 PENDING_APPROVAL
- **前端操作按钮**:
  - PENDING_APPROVAL: 审核 + 编辑 + 删除
  - APPROVED/SHIPPING/DELIVERED/CLOSED: 仅再次下单（复制）
  - 取消按钮已移除
- **前端列表**: 新增"审核人"列，colSpan 同步改为 9
- 后端已重启，前端已重建
