## 2026-07-05

### 审批流 - 连线模式 UI 完善
- **移除旧"分支规则"面板**：条件节点不再显示 `branches` 编辑 UI，改为提示用户使用下方「连线配置」
- **删除废弃函数**：`addBranch`/`removeBranch`/`updateBranch`/`buildCondition` 已删除
- **修复 parseCondition 字段名不匹配**：连线面板中 `parsed.field`/`parsed.op` → `parsed.fieldName`/`parsed.operator`，使条件字段下拉和运算符下拉正常工作
- **修复模板列表渲染**：`t.nodes.map` → 兼容新格式 `{ nodes, connections }` 和旧格式（数组）
- **修复 OPERATORS 渲染 bug**：`key={op} value={op}>{op}</MenuItem>` 对象不能作为 React child，改为 `key={op.value} value={op.value}>{op.label}</MenuItem>`，运算符下拉（≥、≤、>、<、=）不再报错
- Portal 前端已构建，gateway 已重启

### React Flow 画布审批流编辑器
- 安装 `@xyflow/react` 依赖包
- 新增 `FlowCanvas.jsx`（~350行）：画布组件，支持：
  - 6种自定义节点：条件分支（菱形）、角色审批、固定人员、部门主管、会签、或签，各自不同颜色和图标
  - 结束节点（灰色圆角）
  - 自定义连线（ConditionEdge）：连线上显示条件标签（如 "≥ 25"），点击可编辑
  - 条件编辑对话框：简单模式（字段下拉+运算符下拉+值输入）和高级模式（文本框）
  - 工具栏拖拽：从顶部工具栏拖入新节点到画布
  - 连线创建：从节点 handle 拖出连线，自动带默认条件
  - 画布操作：缩放、平移、网格吸附、小地图
- FlowTemplates.jsx 集成：编辑对话框新增「画布/列表」切换按钮，默认画布模式；画布模式下隐藏原有的 DialogActions，保存/取消按钮嵌入画布底部
- 数据双向转换：formToFlow/flowToForm 实现 `{ nodes, connections }` ↔ ReactFlow nodes/edges 互转
- 后端引擎零改动
- **新增双击节点编辑属性**：双击画布上任意节点弹出属性编辑框，可修改名称、类型（角色/固定人员/部门主管/会签/或签/条件）、审批角色（role）、审批人下拉搜索（person）
- **修复画布 Delete 键删不掉**：ReactFlow 缺少 `deleteKeyCode={['Delete', 'Backspace']}` 属性，添加后选中节点/连线按 Delete 或 Backspace 可删除
- **修复保存模板不记录节点位置**：`flowToForm` 新增 `positionX`/`positionY` 字段存储节点坐标；`formToFlow` 加载时优先用存储位置，无位置时 fallback 自动布局。二次打开模板画布布局不变
- **修复结束节点位置不保存**：`flowToForm`/`formToFlow` + FlowTemplates.jsx 完整链路传递 `endPositionX`/`endPositionY`
- **修复审批人幽灵ID**：approverResolver.js 中 `person` 类型现在会校验 `approverUserId` 是否在 portal_user 中存在；无效时按 `approverName` 反查正确 globalId；模板节点25的幽灵ID已修正；卡住的 SO20260705JXKUPE 审批任务已修复

### 基础数据 - 地址管理模块（含高德地图距离计算）
- **Prisma 新增 Address 模型**：originName/originAddress/originLng/originLat + destName/destAddress/destLng/destLat + distance (Decimal 10,2) + status + remark
- **后端路由** `server/src/modules/address/address.routes.js`：
  - CRUD: GET/POST/PUT/DELETE `/api/address`
  - 地理编码: `/api/address/geocode?address=xxx` → 返回经纬度
  - 距离计算: `/api/address/distance?originLng/lat&destLng/lat` → 返回驾车公里数
  - 一键计算: `/api/address/calc-distance?origin=xxx&dest=xxx` → 地理编码+距离一步完成
- **前端页面** `client/src/pages/master/AddressList.jsx`：列表+新增/编辑弹窗，弹窗内「高德地图计算驾车距离」按钮一键获取经纬度和公里数
- **菜单注册**：基础数据 → 地址管理（Route 图标）
- ⚠️ **高德 Key 需配置**：替换 `address.routes.js` 中 `AMAP_KEY` 或设置环境变量 `AMAP_KEY`
- **高德 Key 已配置**：270f0501ec49e95ae86474330ed8d780
- **搜索修复（最终方案）**：前端不再直接调高德 API，改为通过后端代理
  - 后端新增 `GET /api/address/search?keyword=xxx`，代理调用高德 `inputtips` REST API
  - 前端 `MapPicker.jsx` 调用 `api.get('/address/search', { params: { keyword: text } })`，使用 `api.js` 封装自动处理 `/api` vs `/scm/api` 路径前缀和 token 鉴权
  - 架构：`前端 MapPicker → SCM后端 /address/search → 高德 REST API`

### 地址管理 - 高德地图交互选点
- **新增 `MapPicker.jsx` 组件**：加载高德 JS API v2.0，全屏地图 + 搜索框 + 点击选点 + 逆地理编码
- **AddressList.jsx 集成**：起始地和目的地的「详细地址」输入框右侧新增 🗺️ 图标按钮
- 点击按钮→弹出地图对话框→搜索或点击选址→确认后自动回填地址、坐标
- **高德驾车距离计算修复**：前端 `handleCalcDistance` 现在优先使用已有坐标（地图选点获得）调用 `/address/distance`；无坐标时才走 `/address/calc-distance` 做地址文本地理编码。避免已选精确坐标被地址文本模糊匹配覆盖导致的距离偏差

### 地址管理 - MapPicker 修复
- **修复逆地理编码属性名错误**：`result.reinfo` → `result.regeocode`（高德 JS API v2.0 的正确属性名）。之前点击地图后 `pickedAddress` 永远为空，确认按钮无法回填地址
- **修复搜索结果空数组过滤**：高德 `inputtips` 某些结果返回 `location: []` 空数组，添加过滤避免 `NaN` 坐标导致地图报错
- **添加调试日志**：`reverseGeocode` 中添加 `console.log` 输出逆地理编码状态，失败时显示错误提示
- **审批流条件字段扩展**：Portal `FlowTemplates.jsx` 中 `CONDITION_VARIABLES.sales_order` 从 4 个字段扩展到 14 个字段，覆盖销售订单全部主数据字段
  - SCM 后端 `objectData` 新增：warehouseName、salesRepName、orderDate、expectedDate、priceType、taxAmount、grandTotal、status、notes
  - 前端下拉选项：毛利率/订单总金额/含税总额/税额/物料数量/客户名称/仓库名称/业务员/订单编号/订单状态/价格类型/订单日期/期望交货日/备注

### 地址管理 - 未保存变更提示
- 加载 `unsaved-changes-guard` skill，按3步模板实现：
  1. 新增 `confirmClose` 状态 + `hasFormChanges()`（编辑对比原始数据/新增检查非空值）+ `handleCloseDialog()`（有改动弹确认）
  2. Dialog `onClose` 和取消按钮从 `closeDialog` 改为 `handleCloseDialog`
  3. 添加确认关闭弹窗（「继续编辑」/「放弃更改」）
- 保存成功仍直接关闭，不触发确认

### 客户管理数据隔离
- 在 `master.routes.js` 新增 `isDepartmentManager()` 辅助函数（5分钟缓存）
- GET `/customers` 数据隔离规则（多次迭代）：
  - SUPER_ADMIN → 全部
  - **部门负责人（不限角色）** → 查本部门成员 employeeId，`salesRepId IN (...)` 看到部门全部
  - SALES_MANAGER/STAFF/REP 非负责人 → 只看 `salesRepId = 自己`
  - 其他角色 → 只看 `salesRepId = 自己`（如有关联）
- 修正历程：①初版按 departmentId 过滤无效（客户 deptId 为 null）→ ②改为按部门成员 employeeId 集合 → ③非销售角色误返回空列表 → ④部门负责人判断不绑定 SALES_MANAGER，吴月新(PURCHASE_MANAGER)也能看到餐饮事业部全部

- **审批详情修正**：等级显示为"-"（include 缺少 grade）、行金额错误（costPrice*qty→unitPrice*qty）、单位缺失
  - 后端 `sales.routes.js` submit-approval: include 加 `grade: true`，`material` 已含 `salesUnit`；`lineTotal` 修正为 `unitPrice * qty`；`detailItems` 新增 `salesUnit` 字段
  - 前端 `PendingTasks.jsx` 数量/成本价/销售单价后显示 `salesUnit`（如"90 斤"、"¥2.01 /斤"）
  - 行金额 = 数量 × 销售单价（如 90 × 2.50 = ¥225.00）
- `PendingTasks.jsx` 新增「审批详情」弹窗，点击「查看详情」按钮触发
- 显示 `objectData` 主数据：订单编号、客户、业务员、订单日期、总金额、综合毛利率、最低毛利率
- 显示 `objectData.items` 明细表格：物料、等级、数量、成本价、销售单价、行金额、行毛利率
- 审批人可在 Portal 内直接查看完整订单明细，无需跳转 SCM
- **前端列表**综合成本毛利率从加权平均法改为总额法（与后端审批流一致）：旧 `Σ(行毛利率×销售金额)/总销售额` → 新 `(总收入−总成本)/总成本×100`
- **弹窗**中综合成本毛利率同步修正为总额法
- **新增最低成本毛利率**：取所有明细中 `((unitPrice−costPrice)/costPrice×100)` 的最小值，弹窗和列表均已加入

### scm-data-isolation skill
- 沉淀为 `~/.workbuddy/skills/scm-data-isolation/`
- SKILL.md：决策树 + isDepartmentManager 代码 + 适配指南
- `references/isolation-template.md`：完整可复制模板，按模块替换 FILTER_FIELD/TABLE/角色白名单

### 销售订单数据隔离
- 在 `sales.routes.js` GET `/orders` 中按 `salesRepId` 应用数据隔离，复用已有 `isDepartmentManager()`

### 客户信用已用额度修复
- **根因**：`/credit/overview` 和 `/credit/:id` 中 `salesOrders` 状态过滤为 `['DRAFT','CONFIRMED','SHIPPING']`，但实际订单状态全是 `APPROVED`，导致 `pendingOrderAmount` = 0 → `usedCredit` = 0
- **修复**：改为 `['APPROVED', 'PARTIAL_SHIPPED', 'SHIPPED']`（已审批/部分发货/已发货），两处都改了
- 修复后8笔 APPROVED 订单（￥4585.53）会按客户计入已用额度
