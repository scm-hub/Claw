### 16:47 - JobList 权限修复 + 美化
- 「新建职位」按钮从所有人可见恢复为仅 HR_ADMIN + SUPER_ADMIN
- 按钮文字「新建职位」→「新建招聘」，弹窗标题同步
- 全面美化：渐变统计卡片、紫色渐变按钮、隔行斑马纹、首字母头像、紫色渐变弹窗标题栏、分段表单

### 17:03 - 候选人简历附件上传
- Schema: Candidate 加 resumeFile String?；上传中间件支持 PDF/DOC/DOCX 最大 10MB
- 路由: POST /candidates 改用 multipart；新增 GET /candidates/:id/resume 下载
- 前端: 添加弹窗加文件选择器+Chip 预览、详情弹窗加下载按钮、看板卡片加「有简历附件」标识
- CandidateList isAdmin 同时支持 HR_ADMIN + SUPER_ADMIN

### 17:10-17:30 - 面试时间字段样式迭代
- 面试官/面试时间同行，均使用 fullWidth
- 三版迭代：OutlinedInput(边框穿过文字) → Typography外部标签(不符合预期) → TextField+shrink(最终方案)
- 最终：TextField + type="datetime-local" + InputLabelProps={{ shrink: true }}，标签始终在边框上方

### 17:32 - 全系统代码健康检查
- 数据库：Schema 与 MySQL 完全同步 ✅
- 后端：14 个路由模块全部正常加载，app.js 无报错 ✅
- 前端：Vite build 通过，23 个页面文件全部存在，前端 API 调用 100% 匹配后端路由 ✅
- 中间件：auth/rbac/errorHandler/departmentScope/upload 全部正常导出 ✅
- 权限：RBAC 层级体系正确，SUPER_ADMIN(4) > HR_ADMIN(3) > MANAGER(2) > EMPLOYEE(1)

### 17:50 - 部门管理界面全面优化
- 统计卡片：4 列网格（部门总数/一级部门/员工总数/最大层级），渐变图标 Avatar + 彩色顶部装饰条，悬浮上浮效果
- 操作栏：Paper 包裹 + 左右分区 + 部门统计文字概要 + 紫色渐变新建按钮
- 表格头：紫色主题色，每列带对应图标
- 树形行：6 层自动配色（蓝紫绿橙红深紫），首字母彩色头像，层级缩进加左侧竖线引导线
- 负责人：带 ManagerIcon 的彩色 Chip，未指定用虚线边框灰色
- 人数 Chip：悬浮变纯色实心
- 操作按钮：排序/编辑/删除，hover 有对应颜色反馈，排序与编辑间用竖线分隔
- 空状态：大号 DomainIcon + 提示文字 + 快捷新建按钮
- 弹窗：紫色渐变标题栏（带图标+副标题），保存按钮渐变配色，圆角升级
- 导入弹窗：同款渐变标题栏 + 上传区改用紫色主题 + 错误详情纸片美化

### 17:53 - 部门管理按钮全面优化
- 操作栏按钮：导入/导出改用紫色主题 outlined（borderColor + hover 紫色背景 + shadow），新建渐变 contained 加 shadow，全部 textTransform: none + fontWeight 统一
- 弹窗按钮：取消改为 outlined 紫边灰字，保存/导入改用渐变 contained + 统一阴影 + disabled 态半透明紫色
- 下载模板按钮：紫边紫字 outlined + hover 紫底
- 空状态新建按钮：同步渐变 + shadow 风格
- 整体统一：所有按钮 borderRadius: 2，textTransform: none，fontWeight: 600（主按钮）/ 500（次要按钮）

### 17:56 - 修复子部门上级显示为"—"的问题
- 原因：GET /departments 树形查询中 children 的 include 缺少 parent 字段，前端 `dept.parent?.name` 始终为 undefined
- 修复：在 children 和 grandchildren 的 include 中都加上 `parent: { select: { id: true, name: true } }`

### 18:11 - 报表中心全面重构
- **后端**: reports.js 从 2 个接口扩展到 11 个：
  - `GET /dashboard-summary` - 仪表盘摘要
  - `GET /employee-summary` - 员工构成（性别/状态/学历/部门分布 + 近12月入职离职趋势）
  - `GET /attendance-summary` - 考勤统计（出勤率/迟到早退/每日趋势/部门对比）
  - `GET /salary-summary` - 薪资统计（汇总/部门/月度趋势/明细表）
  - `GET /training-summary` - 培训统计（参与率/签到率/部门参训率/员工学时排名）
  - `GET /recruitment-summary` - 招聘统计（阶段漏斗/急聘程度/职位竞争度）
  - `GET /performance-summary` - 绩效统计（评分分布/自评vs上级/趋势）
  - `GET /contract-summary` - 合同统计（到期预警/类型分布/到期趋势）
  - `GET /leave-summary` - 请假统计（类型分布/部门维度）
  - `GET /employee-export` - 员工花名册 Excel 导出
  - `GET /attendance-export` - 考勤 Excel 导出
  - `GET /salary-export` - 薪资 Excel 导出
- **前端**: Reports.jsx 从 2 个卡片升级为 9 个 Tab（📊概览/👥员工/📅考勤/💰薪资/🎓培训/💼招聘/📈绩效/📝合同/🏖️请假）
  - 每个 Tab 含：统计卡片 + ECharts 图表（饼图/柱状图/折线图）+ 数据明细表 + 导出按钮
  - 顶部 5 个动态统计卡（员工总数/今日出勤率/本月入职/合同预警/招聘中职位）
  - 薪资 Tab 仅管理员可见
  - 考勤/薪资/绩效/请假 Tab 支持月份筛选联动查询

### 17:56 - 修复子部门上级显示为"—"的问题
- 原因：GET /departments 树形查询中 children 的 include 缺少 parent 字段，前端 `dept.parent?.name` 始终为 undefined
- 修复：在 children 和 grandchildren 的 include 中都加上 `parent: { select: { id: true, name: true } }`

## 报表中心加载体验优化
- 将所有 CircularProgress 转圈替换为 MUI Skeleton 骨架屏（wave 动画），包括顶部5个统计卡片和9个子Tab面板
- 新增 dashboardLoading 独立状态，概览Tab加载中显示完整骨架占位图
- 新增 TabLoadingSkeleton 组件，子Tab加载中显示卡片+图表+筛选栏骨架
- 移除 LinearProgress 顶条，改为内联骨架占位
- 前端编译验证通过

## SCM 系统迁移至 Claw 目录
- 将 SCM 供应链管理系统 v3.1 从 Library/Application Support 复制到 /Users/pukun/WorkBuddy/Claw/scm/
- 系统文件：index.html, app.js (130K), app-modules.js (186K), mobile.html (230K), style.css (22K)
- 附加资源：scm_schema.sql, scm_data.sql, scm_schema_fixed.sql, SCM实施方案.md
- 15个业务模块：驾驶舱/需求计划/采购/供应商/生产/库存/仓库/运输/销售/质量/财务/数据分析/主数据/系统集成/组织架构/销售计划
- API 后端地址：http://192.168.21.34:8750

## SCM 系统代码审查
- 完整解读 scm/ 下 app.js (2426行) 和 app-modules.js (1910行) 的代码架构
- 发现 20+ 个问题，按严重程度分为 P0(致命) / P1(严重) / P2(中等) / P3(轻微)
- 已输出详细问题清单至会话
